// Este layout ya NO necesita AdminShell — está en admin/layout.js (nivel padre)
// Solo existe para mantener la compatibilidad del grupo de rutas (dashboard)
export default function DashboardGroupLayout({ children }) {
  return children;
}
